# lambda functions are only used for convenience and it's not like that the work can't be done without using lambda functions

# lambda is a one liner function, and it does the same thing that a function does as
minus = lambda x,y:x-y
   # it's equivalent to the below given function
def minus(x,y):
    return x-y